import LayoutMdx from "@/components/layout/LayoutMdx";
import ResponsiveEmbed from "react-responsive-embed";

export const meta = {
  title: "Why we develop Cargoship",
  description: "Cargoship Team why there is a need for a new open source AI model store",
  date: "2023-02-03",
};

This blogpost is still in the works. We will publish it soon.

export default ({ children }) => <LayoutMdx meta={meta}>{children}</LayoutMdx>;
